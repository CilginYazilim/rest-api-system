# Görünüm dosyalarına tarayıcıdan erişim yasaktır.
# Bunlar yalnızca View sınıfı tarafından dahil edilir.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
